server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/keeply
    username: keeply
    password: keeply123
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

keeply:
  jwt:
    secret: "troque-esta-chave-por-uma-chave-com-mais-de-32-caracteres"
    expiration-minutes: 120
  minio:
    endpoint: "http://localhost:9000"
    access-key: "keeply"
    secret-key: "keeply123"
    bucket: "keeply"
