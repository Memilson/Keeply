name: keeply_mobile
description: Keeply Agent Mobile — Backup & Sync
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  web_socket_channel: ^3.0.2
  http: ^1.3.0
  path_provider: ^2.1.0
  shared_preferences: ^2.2.0
  intl: ^0.20.2
  provider: ^6.1.2
  mobile_scanner: ^6.0.0
  crypto: ^3.0.6
  permission_handler: ^11.3.1
  device_info_plus: ^11.2.0
  local_auth: ^2.1.0
  flutter_secure_storage: ^9.0.0
  flutter_svg: ^2.0.7
  uuid: ^4.0.0
  cached_network_image: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
